name: Cook actual district for Android using verified shipped host backend
on:
  workflow_dispatch:
    inputs:
      scene_run:
        description: Verified private scene release run number
        required: true
        type: string
      package_after_cook:
        description: Continue to signed internal APK only after a successful matching cook
        required: false
        default: false
        type: boolean
      native_run:
        description: Successful matching native source run for packaging
        required: false
        default: '36042319324'
        type: string
permissions:
  contents: read
concurrency:
  group: android-real-content-cook
  cancel-in-progress: false
jobs:
  mobile-cook:
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Test dependency integrity and safe extraction
        run: python3 -m unittest discover -s tests/android -v
      - name: Prepare disposable runner capacity and pinned Android SDK
        run: |
          set -euo pipefail
          sudo rm -rf /usr/share/dotnet /usr/local/.ghcup /opt/ghc /opt/hostedtoolcache/CodeQL
          docker image prune -af
          mkdir -p .cache artifacts/android-build
          export JAVA_HOME="$JAVA_HOME_21_X64"
          test -x "$JAVA_HOME/bin/java"
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          printf 'y\ny\ny\ny\ny\ny\ny\ny\ny\ny\n' | "$SDKMANAGER" --licenses >/dev/null
          "$SDKMANAGER" --sdk_root="$ANDROID_HOME" "platforms;android-35" "build-tools;35.0.1" "ndk;27.2.12479018"
          "$JAVA_HOME/bin/java" -version
          cat "$ANDROID_HOME/ndk/27.2.12479018/source.properties"
          python3 - <<'PY'
          import json,os,shutil
          from pathlib import Path
          report={'disk_free_bytes':shutil.disk_usage('/var/lib/docker').free,'ram_bytes':os.sysconf('SC_PHYS_PAGES')*os.sysconf('SC_PAGE_SIZE'),'sdk_platform':35,'ndk':'27.2.12479018','build_tools':'35.0.1','java_major':21}
          Path('artifacts/android-build/host-toolchain.json').write_text(json.dumps(report,indent=2))
          print(report,flush=True)
          assert report['disk_free_bytes']>=85*1024**3, 'Insufficient capacity before engine pull'
          PY
      - name: Pull licensed pinned engine with verified Android host modules
        env:
          DOCKER_CONFIG: ${{ runner.temp }}/ue-registry-auth
          GH_TOKEN: ${{ secrets.UE_REGISTRY_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p "$DOCKER_CONFIG"
          trap 'docker logout ghcr.io >/dev/null 2>&1 || true; rm -rf "$DOCKER_CONFIG"' EXIT
          printf '%s' "$GH_TOKEN" | docker login ghcr.io --username tobirama2904-cell --password-stdin
          IMAGE=$(python3 -c 'import json; print(json.load(open("BuildData/engine.lock.json"))["reference"])')
          timeout --foreground 20m docker pull "$IMAGE"
      - name: Fetch exact private retained scene
        env:
          GH_TOKEN: ${{ secrets.UE_REGISTRY_TOKEN }}
          SCENE_RUN: ${{ inputs.scene_run }}
        run: |
          [[ "$SCENE_RUN" =~ ^[0-9]+$ ]]
          mkdir -p .cache/private-scene
          READY=0
          for ATTEMPT in $(seq 1 30); do
            COUNT=$(gh release view "scene-$SCENE_RUN" --repo tobirama2904-cell/poco-ue5-engine-cache --json assets --jq '.assets | map(select(.name == "game-content.tar.gz" or .name == "SHA256SUMS")) | length' 2>/dev/null || echo 0)
            if [[ "$COUNT" == 2 ]]; then READY=1; break; fi
            echo "Waiting for the exact scene checkpoint; attempt $ATTEMPT/30"
            sleep 20
          done
          test "$READY" = 1
          gh release download "scene-$SCENE_RUN" --repo tobirama2904-cell/poco-ue5-engine-cache --dir .cache/private-scene --pattern game-content.tar.gz --pattern SHA256SUMS
      - name: Build game editor module and actually cook district for Android ASTC
        run: |
          set -euo pipefail
          IMAGE=$(python3 -c 'import json; print(json.load(open("BuildData/engine.lock.json"))["reference"])')
          CA_STORE=$(readlink -f "$JAVA_HOME_21_X64/lib/security/cacerts")
          test -s "$CA_STORE"
          sudo chown -R 1000:1000 "$GITHUB_WORKSPACE"
          docker run --rm --user root --entrypoint /bin/bash --cpus=4 --memory=13g --memory-swap=13g --shm-size=1g \
            --mount "type=bind,src=$GITHUB_WORKSPACE,dst=/project" \
            --mount "type=bind,src=$ANDROID_HOME,dst=/opt/android,readonly" \
            --mount "type=bind,src=$JAVA_HOME_21_X64,dst=/opt/java21,readonly" \
            --mount "type=bind,src=$CA_STORE,dst=/opt/java-truststore/cacerts,readonly" \
            -e JAVA_TOOL_OPTIONS=-Djavax.net.ssl.trustStore=/opt/java-truststore/cacerts \
            -e ANDROID_HOME=/opt/android -e ANDROID_SDK_ROOT=/opt/android \
            -e NDKROOT=/opt/android/ndk/27.2.12479018 -e JAVA_HOME=/opt/java21 \
            "$IMAGE" -lc 'apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y -qq mesa-vulkan-drivers libvulkan1 xvfb xauth vulkan-tools && runuser -u ue4 -- bash /project/tools/android/cook_host.sh'
      - name: Retain cooked content and licensed host modules privately
        env:
          GH_TOKEN: ${{ secrets.UE_REGISTRY_TOKEN }}
        run: |
          test "$(gh api repos/tobirama2904-cell/poco-ue5-engine-cache --jq .private)" = true
          gh release create "mobile-cook-${GITHUB_RUN_ID}" .cache/private-cook/* --repo tobirama2904-cell/poco-ue5-engine-cache --prerelease --title "Private mobile content cook ${GITHUB_RUN_ID}" --notes 'Actual Android ASTC cooking checkpoint; not an APK or final campaign.'
      - name: Continue verified cooking into internal APK packaging
        if: inputs.package_after_cook
        env:
          GH_TOKEN: ${{ secrets.UE_REGISTRY_TOKEN }}
          NATIVE_RUN: ${{ inputs.native_run }}
        run: |
          [[ "$NATIVE_RUN" =~ ^[0-9]+$ ]]
          gh workflow run android-package.yml --ref "$GITHUB_REF_NAME" -f native_run="$NATIVE_RUN" -f cook_run="$GITHUB_RUN_ID"
      - name: Upload diagnostics only, never licensed libraries or editable assets
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: mobile-cook-diagnostics-${{ github.run_id }}
          path: |
            artifacts/android-build/*.json
            artifacts/android-build/*.log
            artifacts/engine-probe/*.json
            artifacts/gameplay-scene/*.json
            artifacts/gameplay-scene/*.log
            artifacts/gameplay-scene/*.png
          retention-days: 7
